apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cupdate
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: cupdate
    server: "https://kubernetes.default.svc"
  source:
    path: gitops/manifests/cupdate
    repoURL: "https://github.com/timmyb824/homelab-kubernetes-apps.git"
    targetRevision: HEAD
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
