apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: homelab-live-status
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: homelab-live-status
    server: "https://kubernetes.default.svc"
  source:
    path: gitops/manifests/homelab-live-status
    repoURL: "https://github.com/timmyb824/homelab-kubernetes-apps.git"
    targetRevision: HEAD
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
