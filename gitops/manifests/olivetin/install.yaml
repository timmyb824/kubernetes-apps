---
# create namespace
apiVersion: v1
kind: Namespace
metadata:
  name: olivetin
---
apiVersion: v1
kind: Secret
metadata:
  name: olivetin-ssh-key
  namespace: olivetin
type: Opaque
data:
  id_master_key_nopass: <path:secret/data/olivetin#ID_MASTER_KEY_NOPASS_PRIVATE>
  id_master_key_nopass.pub: <path:secret/data/olivetin#ID_MASTER_KEY_NOPASS_PUBLIC>
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: olivetin-config
data:
  config.yaml: |
    logLevel: INFO

    prometheus:
        enabled: true
        defaultGoMetrics: false

    actions:
      - title: ping the internet
        shell: ping 1.1.1.1 -c 1
        icon: ping
        timeout: 3

      - title: whoami on hp-laptop-ubuntu
        shell: ssh -i /home/olivetin/.ssh/id_master_key_nopass tbryant@192.168.86.53 'whoami'
        icon: ping
        timeout: 5

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: olivetin
  namespace: olivetin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: olivetin
  template:
    metadata:
      labels:
        app: olivetin
        app.kubernetes.io/name: olivetin
    spec:
      containers:
        - name: olivetin
          image: docker.io/jamesread/olivetin:2024.09.11
          command: ["/bin/sh", "-c"]
          args:
            - |
              # Debug: List contents of /home/olivetin
              echo "Contents of /home/olivetin:"
              ls -la /home/olivetin

              # Debug: List contents of /home/olivetin/.ssh
              echo "Contents of /home/olivetin/.ssh:"
              ls -la /home/olivetin/.ssh

              # Set permissions
              chmod 700 /home/olivetin/.ssh
              chmod 600 /home/olivetin/.ssh/*
              chown -R olivetin:olivetin /home/olivetin/.ssh

              # Move and set permissions for the private key
              mv /home/olivetin/.ssh/id_master_key_nopass /home/olivetin/id_master_key_nopass
              chown olivetin:olivetin /home/olivetin/id_master_key_nopass
              chmod 600 /home/olivetin/id_master_key_nopass

              # Debug: List contents after changes
              echo "Contents of /home/olivetin after changes:"
              ls -la /home/olivetin
              echo "Contents of /home/olivetin/.ssh after changes:"
              ls -la /home/olivetin/.ssh

              # Start OliveTin as olivetin user
              exec su -s /bin/sh olivetin -c "/usr/local/bin/olivetin"
          ports:
            - containerPort: 1337
          volumeMounts:
            - name: olivetin-config
              mountPath: "/config"
              readOnly: true
            - name: ssh-key-private
              mountPath: "/home/olivetin/.ssh/id_master_key_nopass"
              subPath: id_master_key_nopass
            - name: ssh-key-public
              mountPath: "/home/olivetin/.ssh/id_master_key_nopass.pub"
              subPath: id_master_key_nopass.pub
          livenessProbe:
            exec:
              command:
                - curl
                - localhost:1337
            initialDelaySeconds: 5
            periodSeconds: 30
      volumes:
        - name: olivetin-config
          configMap:
            name: olivetin-config
        - name: ssh-key-private
          secret:
            secretName: olivetin-ssh-key
            items:
              - key: id_master_key_nopass
                path: id_master_key_nopass
            defaultMode: 0600
        - name: ssh-key-public
          secret:
            secretName: olivetin-ssh-key
            items:
              - key: id_master_key_nopass.pub
                path: id_master_key_nopass.pub
            defaultMode: 0644
---
apiVersion: v1
kind: Service
metadata:
  name: olivetin
  namespace: olivetin
spec:
  selector:
    app: olivetin
  ports:
    - protocol: TCP
      port: 1337
      targetPort: 1337
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: olivetin-ingress
  namespace: olivetin
  annotations:
    kubernetes.io/ingress.class: traefik-external
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`olivetin.local.timmybtech.com`)
      kind: Rule
      services:
        - name: olivetin
          port: 1337
  tls:
    secretName: local-timmybtech-com-tls
