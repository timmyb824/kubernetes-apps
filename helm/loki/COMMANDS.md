# Commands

```bash
helm show values loki/loki > loki_values.yaml
helm show values loki/promtail > promtail_values.yaml
helm show values loki/loki-stack > loki_stack_values.yaml
helm show values loki/fluent-bit > fluent-bit.yaml
helm show values grafana/grafana > grafana.yaml
```